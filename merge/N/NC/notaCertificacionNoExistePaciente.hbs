{{#define id="notaCertificacionNoExistePaciente"}}
  {{action 
    id="afectar"
    confirm="=fn('validarAltaPaciente', base.paciente)"
    confirmWidth="400"
    confirmHeight="500"
    confirmMessage="=fn('mensajeAltaPaciente', base.paciente)"
  }}

  {{#section id="base"}}
    {{validator type="expr" validIf="=verificacionBusqueda=='si'" message="Falta Certificar"}}
  {{/section}}
{{/define}}